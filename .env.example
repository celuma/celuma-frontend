# Base URL used by the app at runtime (must start with VITE_ prefix)
VITE_API_BASE_URL=http://localhost:8000/api

# Dev proxy target for Vite (used only in dev server)
VITE_PROXY_TARGET=http://localhost:8000

# Docker compose variable
BACKEND_URL=http://backend:8000

#backend
APP_NAME=celuma
ENV=dev
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/celumadb
JWT_SECRET=changeme
JWT_EXPIRES_MIN=480

AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=xxxxxxxx
AWS_REGION=mx-central-1
S3_BUCKET_NAME=your-bucket

MEDIA_PUBLIC_BASE_URL=https://dxxxxxxxxxxxxx.cloudfront.net

# MEDIA_PRESIGNED_EXPIRE_SECONDS=3600
# S3_ENDPOINT_URL=http://localhost:4566
# Optional alternative env name used in vite.config.ts
VITE_API_BACKEND_HOST=
